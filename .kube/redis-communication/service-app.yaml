apiVersion: v1
kind: Service
metadata:
  name: redis-communication-service
spec:
  selector:
    app: redis-communication
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30082
  type: NodePort
